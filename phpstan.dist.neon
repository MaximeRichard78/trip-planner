parameters:
    level: 5
    paths:
        - src
    excludePaths:
        analyseAndScan:
            - var (?)
            - vendor (?)
    ignoreErrors:
        - '#Property App\\Entity\\.*::\$id \(int\|null\) is never assigned int so it can be removed from the property type\.#'